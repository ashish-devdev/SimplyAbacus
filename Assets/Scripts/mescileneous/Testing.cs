using System.Collections;
using System.Collections.Generic;
using TMPro;
using UnityEngine;

public class Testing : MonoBehaviour
{

    void Start()
    {
        
    }

    void Update()
    {
        if (Input.GetMouseButtonDown(0))
        {
            //print("string" + GetComponent<TextMeshProUGUI>().text);
            //print("color" + GetComponent<TextMeshProUGUI>().color);
            //print("gameobject enable?" + gameObject.activeInHierarchy) ;
            //print("TMP enable?" + GetComponent<TextMeshProUGUI>().enabled) ;
            //print("parent enable?" +gameObject.transform.parent.transform.gameObject.activeInHierarchy) ;
            
        }
    }
}
